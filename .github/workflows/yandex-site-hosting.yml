name: Yandex site hosting

# Переезд сайта с GitHub Pages в Yandex Object Storage.
# Из песочницы агента облако недоступно (сеть закрыта политикой), а здесь
# открыто — и ключи YANDEX_S3_* уже есть в Secrets: ими автосинк заливает медиа.
# Поэтому переезд ведём отсюда, по шагам, каждый запускается вручную.
#
#   probe   — что умеют текущие ключи: видят ли бакеты, хватает ли прав на создание
#   create  — создать бакет сайта и включить режим статического хостинга
#   deploy  — залить готовый сайт в бакет (без медиа и исходников)
#   verify  — проверить, что по адресу бакета отдаётся живой сайт
#
# Домен не трогается ни на одном шаге: переключение DNS — отдельное решение
# владелицы, до него сайт живёт на GitHub Pages как и раньше.

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Шаг: probe / create / deploy / verify / cert-request / cert-status / cdn-grant / cdn-probe / cdn-create / cdn-status / dns-snapshot / domain-check / redirect-create / cert-request-com / cert-status-com / cdn-create-com / cdn-status-com / redirect-verify / dns-create-com / dns-status-com / media-audit / media-optimize / redirect-create-ru / cert-request-ru / cert-status-ru / dns-create-ru / dns-status-ru / cdn-create-ru / cdn-status-ru / redirect-verify-ru"
        required: true
        default: "probe"
      bucket:
        description: "Имя бакета для сайта"
        required: false
        default: "abhazbereg-site"
      acme_value:
        description: "Для dns-create-ru: CNAME-значение проверки сертификата (…cm.yandexcloud.net.)"
        required: false
        default: ""
      cert_name:
        description: "Для media-шагов: имя сертификата вместо abhazbereg-media-cdn (перевыпуск под новым именем)"
        required: false
        default: ""

env:
  ENDPOINT: https://storage.yandexcloud.net
  AWS_ACCESS_KEY_ID: ${{ secrets.YANDEX_S3_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.YANDEX_S3_SECRET_ACCESS_KEY }}
  AWS_DEFAULT_REGION: ru-central1
  BUCKET: ${{ inputs.bucket }}

jobs:
  hosting:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Установить boto3
        if: inputs.action == 'deploy' || inputs.action == 'verify'
        run: pip install --quiet boto3

      - name: Проверить, что ключи заданы
        run: |
          if [ -z "$AWS_ACCESS_KEY_ID" ] || [ -z "$AWS_SECRET_ACCESS_KEY" ]; then
            echo "Нет секретов YANDEX_S3_ACCESS_KEY_ID / YANDEX_S3_SECRET_ACCESS_KEY."
            exit 1
          fi
          echo "Ключи на месте (значения не печатаем)."

      # Разведка: понять, что можно сделать этими ключами, ничего не меняя.
      - name: probe — что умеют ключи
        if: inputs.action == 'probe'
        run: |
          echo "=== Бакеты, которые видит ключ ==="
          aws s3 ls --endpoint-url "$ENDPOINT" || echo "Список бакетов недоступен (роль уже, чем storage.admin)."

          echo
          echo "=== Чтение бакета медиа (должно работать — им пользуется автосинк) ==="
          aws s3 ls s3://abhazbereg-media/media/ --endpoint-url "$ENDPOINT" | head -5 \
            || echo "ВНИМАНИЕ: даже медиа не читается, ключ не тот."

          echo
          echo "=== Существует ли уже бакет сайта «$BUCKET» ==="
          if aws s3 ls "s3://$BUCKET" --endpoint-url "$ENDPOINT" >/dev/null 2>&1; then
            echo "Бакет уже есть — шаг create не нужен."
          else
            echo "Бакета нет. Проверяю право на создание пробным бакетом..."
            PROBE="abhazbereg-probe-$(date +%s)"
            if aws s3 mb "s3://$PROBE" --endpoint-url "$ENDPOINT" 2>&1; then
              echo "ПРАВА НА СОЗДАНИЕ ЕСТЬ. Убираю пробный бакет."
              aws s3 rb "s3://$PROBE" --endpoint-url "$ENDPOINT"
            else
              echo "ПРАВ НА СОЗДАНИЕ НЕТ — бакет должна создать владелица в консоли."
            fi
          fi

      - name: create — бакет и режим статического сайта
        if: inputs.action == 'create'
        run: |
          if aws s3 ls "s3://$BUCKET" --endpoint-url "$ENDPOINT" >/dev/null 2>&1; then
            echo "Бакет «$BUCKET» уже существует — создавать нечего."
          else
            aws s3 mb "s3://$BUCKET" --endpoint-url "$ENDPOINT"
            echo "Бакет «$BUCKET» создан."
          fi
          # Публичное чтение объектов: сайт должен открываться всем.
          # Право на запись остаётся только у ключа, он в Secrets.
          aws s3api put-bucket-acl --bucket "$BUCKET" --acl public-read \
            --endpoint-url "$ENDPOINT" && echo "Публичное чтение включено."
          aws s3 website "s3://$BUCKET" --index-document index.html \
            --error-document 404.html --endpoint-url "$ENDPOINT" \
            && echo "Режим статического сайта включён: index.html + 404.html."

      - name: deploy — залить сайт в бакет
        if: inputs.action == 'deploy'
        run: python3 tools/deploy_site_to_yandex.py --bucket "$BUCKET"

      - name: verify — проверить сайт по адресу бакета
        if: inputs.action == 'verify'
        run: python3 tools/deploy_site_to_yandex.py --bucket "$BUCKET" --verify-only

      # Сертификаты — отдельная служба облака, ключи от хранилища к ней не подходят.
      # Нужен авторизованный ключ сервисного аккаунта в секрете YC_SA_KEY_JSON.
      - name: Установить PyJWT
        if: startsWith(inputs.action, 'cert-')
        run: pip install --quiet pyjwt cryptography

      - name: cert-request — заказать сертификат на домен
        if: inputs.action == 'cert-request'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: python3 tools/yc_certificate.py request

      - name: cert-status — что с сертификатом и чего он ждёт
        if: inputs.action == 'cert-status'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: python3 tools/yc_certificate.py status

      # CDN — единственное место, куда прикрепляется наш сертификат: бакет
      # отдаёт свой домен только по HTTP. Домен на CDN не переключается.
      - name: Установить PyJWT (CDN)
        if: startsWith(inputs.action, 'cdn-')
        run: pip install --quiet pyjwt cryptography

      - name: cdn-grant — попробовать выдать роль cdn.editor
        if: inputs.action == 'cdn-grant'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: python3 tools/yc_cdn.py grant

      - name: cdn-probe — хватает ли прав и что уже создано
        if: inputs.action == 'cdn-probe'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          SITE_BUCKET: ${{ inputs.bucket }}
        run: python3 tools/yc_cdn.py probe

      - name: cdn-create — создать ресурс CDN с сертификатом
        if: inputs.action == 'cdn-create'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          SITE_BUCKET: ${{ inputs.bucket }}
        run: python3 tools/yc_cdn.py create

      # Только чтение: список записей зоны до переноса и для сверки после.
      - name: dns-snapshot — что сейчас в зоне домена
        if: inputs.action == 'dns-snapshot'
        run: |
          python3 tools/dns_zone_snapshot.py
          echo
          echo "############ Вторая зона: редиректор ############"
          ZONE=abhazbereg.com python3 tools/dns_zone_snapshot.py
          echo
          echo "############ Третья зона: старая Тильда ############"
          ZONE=abhazbereg.ru python3 tools/dns_zone_snapshot.py

      # Откуда 2.5 ТБ исходящего трафика в месяц: считаем вес медиа по типам
      # и ищем видео без сжатой -web-копии — главных кандидатов на пережатие.
      - name: media-audit — что весит в бакете и что качают гости
        if: inputs.action == 'media-audit'
        run: |
          aws s3 ls s3://abhazbereg-media/media/ --recursive --endpoint-url "$ENDPOINT" \
            > /tmp/listing.txt
          python3 - <<'PYEOF'
          from pathlib import Path
          from collections import defaultdict
          rows = []
          for line in Path("/tmp/listing.txt").read_text().splitlines():
              parts = line.split(None, 3)
              if len(parts) == 4 and parts[2].isdigit():
                  rows.append((int(parts[2]), parts[3]))
          total = sum(size for size, _ in rows)
          print(f"Объектов: {len(rows)}, всего: {total/1e9:.2f} ГБ\n")

          by_ext = defaultdict(lambda: [0, 0])
          for size, key in rows:
              ext = ("." + key.rsplit(".", 1)[-1].lower()) if "." in key else "(без)"
              by_ext[ext][0] += size
              by_ext[ext][1] += 1
          print("По типам:")
          for ext, (size, count) in sorted(by_ext.items(), key=lambda kv: -kv[1][0])[:10]:
              print(f"  {ext:8} {size/1e9:8.2f} ГБ  ({count} файлов)")

          videos = [(s, k) for s, k in rows if k.lower().endswith(".mp4")]
          keys = {k for _, k in rows}
          heavy_raw = [(s, k) for s, k in videos
                       if "-web" not in k and k.replace(".mp4", "-web.mp4") not in keys]
          heavy_raw.sort(reverse=True)
          print(f"\nВидео всего: {sum(s for s,_ in videos)/1e9:.2f} ГБ ({len(videos)} шт)")
          print(f"Несжатых без -web-копии: {sum(s for s,_ in heavy_raw)/1e9:.2f} ГБ "
                f"({len(heavy_raw)} шт). Топ-25:")
          for size, key in heavy_raw[:25]:
              print(f"  {size/1e6:8.1f} МБ  {key}")

          print("\nТоп-15 самых тяжёлых объектов вообще:")
          for size, key in sorted(rows, reverse=True)[:15]:
              print(f"  {size/1e6:8.1f} МБ  {key}")
          PYEOF

      # Экономия трафика: у старых видео мог отсутствовать кэш-заголовок
      # (повторный просмотр гостя тогда качается заново за наши деньги),
      # а исходники -source.mp4 гости не читают вовсе — им место в холодном
      # хранилище. Страницы не трогаются, адреса файлов не меняются.
      - name: media-optimize — кэш видео и исходники в холод
        if: inputs.action == 'media-optimize'
        run: |
          pip install --quiet boto3
          python3 - <<'PYEOF'
          import boto3, mimetypes, os

          s3 = boto3.client("s3", endpoint_url=os.environ["ENDPOINT"])
          BUCKET = "abhazbereg-media"

          keys = []
          token = None
          while True:
              kwargs = {"Bucket": BUCKET, "Prefix": "media/"}
              if token:
                  kwargs["ContinuationToken"] = token
              page = s3.list_objects_v2(**kwargs)
              keys += [(o["Key"], o.get("StorageClass", "STANDARD"))
                       for o in page.get("Contents", [])]
              token = page.get("NextContinuationToken")
              if not token:
                  break
          print(f"Объектов: {len(keys)}")

          GOOD = "public, max-age=604800"

          def recopy(key, storage_class=None):
              extra = {
                  "CopySource": {"Bucket": BUCKET, "Key": key},
                  "Bucket": BUCKET, "Key": key,
                  "MetadataDirective": "REPLACE",
                  "ContentType": mimetypes.guess_type(key)[0] or "video/mp4",
                  "CacheControl": GOOD,
                  "ACL": "public-read",
              }
              if storage_class:
                  extra["StorageClass"] = storage_class
              s3.copy_object(**extra)

          fixed = cooled = ok = 0
          videos = [(k, sc) for k, sc in keys if k.endswith(".mp4")]
          print(f"Видео: {len(videos)}")
          for key, storage_class in videos:
              if key.endswith("-source.mp4"):
                  if storage_class != "COLD":
                      recopy(key, "COLD")
                      cooled += 1
                  continue
              head = s3.head_object(Bucket=BUCKET, Key=key)
              if head.get("CacheControl"):
                  ok += 1
              else:
                  recopy(key)
                  fixed += 1
          print(f"Рабочие видео: с кэшем было {ok}, починено {fixed}")
          print(f"Исходников переведено в холод: {cooled}")

          # Фото: полный проход в потоки — выборка 11.08 показала, что без
          # заголовка примерно каждое третье (старые заливки).
          from concurrent.futures import ThreadPoolExecutor

          photos = [k for k, _ in keys if k.endswith((".webp", ".jpg", ".png"))]
          print(f"Фото: {len(photos)}")

          def fix_photo(key):
              try:
                  if s3.head_object(Bucket=BUCKET, Key=key).get("CacheControl"):
                      return 0
                  recopy(key)
                  return 1
              except Exception as error:
                  print(f"  ошибка {key}: {error}")
                  return 0

          with ThreadPoolExecutor(max_workers=12) as pool:
              photo_fixed = sum(pool.map(fix_photo, photos))
          print(f"Фото починено: {photo_fixed} из {len(photos)}")
          PYEOF

      # ===== Редиректор abhazbereg.com: «будочка» переезжает в Россию =====
      # Старая будочка на GitHub Pages переадресует только главную, а глубокие
      # ссылки /hotels/... отдают голый 404 (найдено Codex 10.08.2026). Новая —
      # пустой бакет, чья website-настройка отвечает 301 на абхазберег.рф с
      # сохранением пути. DNS домена не трогается ни одним из шагов.
      - name: redirect-create — бакет-будочка с 301 на основной домен
        if: inputs.action == 'redirect-create'
        run: |
          RB="abhazbereg-com-redirect"
          echo "=== Сначала: жив ли резерв GitHub Pages (проверка отчёта Codex) ==="
          # dariatravel.github.io отдаёт 404 закономерно: репозиторий настроен
          # на кастомный домен. Резерв проверяется запросом с именем сайта
          # напрямую к адресам GitHub.
          CODE=$(curl -sS -o /dev/null -w "%{http_code}" --max-time 20 \
            --resolve "xn--80aacbklan7f0b.xn--p1ai:443:185.199.108.153" \
            "https://xn--80aacbklan7f0b.xn--p1ai/" || echo 000)
          echo "GitHub Pages по прямому адресу с нашим именем: HTTP $CODE"

          echo
          echo "=== Бакет-будочка ==="
          if aws s3 ls "s3://$RB" --endpoint-url "$ENDPOINT" >/dev/null 2>&1; then
            echo "Бакет $RB уже есть."
          else
            aws s3 mb "s3://$RB" --endpoint-url "$ENDPOINT" && echo "Бакет $RB создан."
          fi
          # ACL может быть недоступен этому ключу — не страшно: будочка не
          # отдаёт файлы, она только отвечает «переезжайте». Судим не по
          # флажку, а по фактическому 301 ниже.
          aws s3api put-bucket-acl --bucket "$RB" --acl public-read \
            --endpoint-url "$ENDPOINT" 2>/dev/null \
            && echo "Публичное чтение включено." \
            || echo "ACL выставить нельзя (AccessDenied) — проверяем, нужен ли он вообще."
          aws s3api put-bucket-website --bucket "$RB" --endpoint-url "$ENDPOINT" \
            --website-configuration '{"RedirectAllRequestsTo":{"HostName":"xn--80aacbklan7f0b.xn--p1ai","Protocol":"https"}}'
          echo "Website-настройка: все запросы → 301 на основной домен."

          echo
          echo "=== Проверка 301 с сохранением пути (это и есть критерий успеха) ==="
          sleep 10
          FAIL=0
          for P in "/" "/hotels/lazurit-novyy-otel-s-zavtrakami-2583/" "/kvartira/akira-2k-kvartira-1623/?x=1"; do
            R=$(curl -sS -o /dev/null -w "%{http_code} -> %{redirect_url}" --max-time 20 \
              "http://$RB.website.yandexcloud.net$P" || echo "ошибка")
            echo "  $P"
            echo "     $R"
            case "$R" in 301*xn--80aacbklan7f0b*|302*xn--80aacbklan7f0b*) ;; *) FAIL=1;; esac
          done
          if [ "$FAIL" = "1" ]; then
            echo "Переадресация не работает — смотри выше."
            exit 1
          fi
          echo "Будочка отвечает правильно."

      - name: cert-request-com — сертификат для abhazbereg.com
        if: inputs.action == 'cert-request-com'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CERT_NAME: abhazbereg-com
          CERT_DOMAIN: abhazbereg.com
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_certificate.py request

      - name: cert-status-com — что с сертификатом abhazbereg.com
        if: inputs.action == 'cert-status-com'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CERT_NAME: abhazbereg-com
          CERT_DOMAIN: abhazbereg.com
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_certificate.py status

      - name: cdn-create-com — CDN-ресурс редиректора
        if: inputs.action == 'cdn-create-com'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: abhazbereg.com
          CDN_CERT_NAME: abhazbereg-com
          CDN_GROUP_NAME: abhazbereg-com-origin
          CDN_ORIGIN_HOST: abhazbereg-com-redirect.website.yandexcloud.net
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py create

      # Проверка будочки через CDN до всякого DNS: подменяем адрес, как это
      # сделает DNS после переключения. Балансировщик CDN раздаёт РАЗНЫЕ
      # адреса — Codex 11.08 поймал узел, который вёл себя иначе, чем другой.
      # Поэтому собираем адреса несколькими запросами и проверяем КАЖДЫЙ:
      # один плохой узел из десяти — это каждый десятый гость с ошибкой.
      - name: redirect-verify — будочка через ВСЕ узлы CDN
        if: inputs.action == 'redirect-verify'
        run: |
          CN="062ff875d101b985.topology.gslb.yccdn.ru"
          IPS=$( (for i in 1 2 3 4 5 6; do dig +short "$CN" @8.8.8.8; dig +short "$CN" @77.88.8.8; sleep 1; done) \
            | grep -E "^[0-9.]+$" | sort -u)
          echo "Адреса, которые раздаёт балансировщик:"; echo "$IPS" | sed 's/^/  /'
          FAIL=0
          for IP in $IPS; do
            echo
            echo "=== Узел $IP ==="
            for P in "/" "/hotels/lazurit-novyy-otel-s-zavtrakami-2583/"; do
              OUT=$(curl -sSI --max-time 25 --resolve "abhazbereg.com:443:$IP" \
                "https://abhazbereg.com$P" 2>&1 | grep -iE "^(HTTP|location|server)" | tr '\n' ' ')
              echo "  $P"
              echo "     $OUT"
              case "$OUT" in *301*xn--80aacbklan7f0b*) ;; *) echo "     ^^ ПЛОХОЙ УЗЕЛ"; FAIL=1;; esac
            done
          done
          echo
          [ "$FAIL" = "0" ] && echo "ИТОГ: все узлы отвечают правильным 301." \
            || { echo "ИТОГ: есть узлы со старой маршрутизацией — переключать нельзя."; exit 1; }

      - name: dns-create-com — зона abhazbereg.com в Cloud DNS
        if: inputs.action == 'dns-create-com'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_dns.py create

      - name: dns-status-com — записи зоны и серверы имён для Рег.ру
        if: inputs.action == 'dns-status-com'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_dns.py status

      # ===== Переезд старого сайта на Тильде: abhazbereg.ru =====
      # Схема как у abhazbereg.com, но пути Тильды не совпадают с новым
      # сайтом, поэтому вместо «всё на новый домен» — точечный 301 на каждый
      # адрес из data/old-site-redirects.json (объекты бакета с
      # x-amz-website-redirect-location).

      - name: redirect-create-ru — будочка с точечными переездами
        if: inputs.action == 'redirect-create-ru'
        run: pip install --quiet boto3 && python3 tools/build_ru_redirector.py

      - name: cert-request-ru — сертификат для abhazbereg.ru
        if: inputs.action == 'cert-request-ru'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CERT_NAME: abhazbereg-ru
          CERT_DOMAIN: abhazbereg.ru
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_certificate.py request

      - name: cert-status-ru — что с сертификатом abhazbereg.ru
        if: inputs.action == 'cert-status-ru'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CERT_NAME: abhazbereg-ru
          CERT_DOMAIN: abhazbereg.ru
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_certificate.py status

      - name: dns-create-ru — зона abhazbereg.ru в Cloud DNS
        if: inputs.action == 'dns-create-ru'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          DNS_ZONE: abhazbereg.ru.
          DNS_ZONE_NAME: abhazbereg-ru
          DNS_ACME_VALUE: ${{ inputs.acme_value }}
        run: |
          if [ -z "$DNS_ACME_VALUE" ]; then
            echo "::error::Передайте acme_value из вывода cert-request-ru."; exit 1
          fi
          pip install --quiet pyjwt cryptography && python3 tools/yc_dns.py create

      - name: dns-status-ru — записи зоны и серверы имён для регистратора
        if: inputs.action == 'dns-status-ru'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          DNS_ZONE: abhazbereg.ru.
          DNS_ZONE_NAME: abhazbereg-ru
          DNS_ACME_VALUE: ${{ inputs.acme_value }}
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_dns.py status

      - name: cdn-create-ru — CDN-ресурс редиректора Тильды
        if: inputs.action == 'cdn-create-ru'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: abhazbereg.ru
          CDN_CERT_NAME: abhazbereg-ru
          CDN_GROUP_NAME: abhazbereg-ru-origin
          CDN_ORIGIN_HOST: abhazbereg-ru-redirect.website.yandexcloud.net
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py create

      - name: cdn-status-ru — состояние CDN редиректора Тильды
        if: inputs.action == 'cdn-status-ru'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: abhazbereg.ru
          CDN_CERT_NAME: abhazbereg-ru
          CDN_GROUP_NAME: abhazbereg-ru-origin
          CDN_ORIGIN_HOST: abhazbereg-ru-redirect.website.yandexcloud.net
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py status

      # Как redirect-verify, но проверяем ТОЧЕЧНЫЕ переезды: карточка → своя
      # страница, статья → своя статья, и на каждом узле балансировщика.
      - name: redirect-verify-ru — будочка Тильды через ВСЕ узлы CDN
        if: inputs.action == 'redirect-verify-ru'
        run: |
          CN="062ff875d101b985.topology.gslb.yccdn.ru"
          IPS=$( (for i in 1 2 3 4 5 6; do dig +short "$CN" @8.8.8.8; dig +short "$CN" @77.88.8.8; sleep 1; done) \
            | grep -E "^[0-9.]+$" | sort -u)
          echo "Адреса балансировщика:"; echo "$IPS" | sed 's/^/  /'
          FAIL=0
          # Хранилище Яндекса не умеет объектные 301, поэтому будочка отдаёт
          # страницы-переезды: 200 + meta refresh на точный адрес. Проверяем
          # тело ответа.
          check_node() {
            IP="$1"; P="$2"; WANT="$3"
            # || true: ошибка TLS/сети — это «плохой узел», а не повод уронить
            # весь скрипт (bash -e прервал проверку 16.08 на curl exit 60).
            BODY=$(curl -sS --max-time 25 --resolve "abhazbereg.ru:443:$IP" \
              "https://abhazbereg.ru$P" 2>&1 || true)
            echo "  $P"
            case "$BODY" in *"xn--80aacbklan7f0b.xn--p1ai$WANT"*) echo "     переезд на $WANT: да";; \
              *) echo "     ^^ ПЛОХО: нет переезда на $WANT ($(echo "$BODY" | head -c 120))"; FAIL=1;; esac
          }
          for IP in $IPS; do
            echo
            echo "=== Узел $IP ==="
            check_node "$IP" "/" "/"
            check_node "$IP" "/tproduct/994323046982-grant-otel" "/hotels/grant-otel-nomera-2820/"
            check_node "$IP" "/tpost/granica" "/blog/kak-projti-granicu-psou/"
            check_node "$IP" "/page62959751.html" "/pitsunda/"
          done
          echo
          [ "$FAIL" = "0" ] && echo "ИТОГ: все узлы отвечают точным 301." \
            || { echo "ИТОГ: есть плохие узлы — переключать нельзя."; exit 1; }

      # ===== Медиа через CDN: media.абхазберег.рф поверх бакета abhazbereg-media =====
      # Зачем: 2,1 ТБ/мес фото и видео идут напрямую из Object Storage по
      # 1,514₽/ГБ (68% счёта). Через CDN тот же трафик стоит 1,054₽/ГБ,
      # кэш на краях режет origin-трафик, гостям быстрее. Решение Дарьи 18.08.

      - name: cert-request-media — сертификат для media.абхазберег.рф
        if: inputs.action == 'cert-request-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CERT_NAME: ${{ inputs.cert_name || 'abhazbereg-media-cdn' }}
          CERT_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
          CERT_NO_WWW: "1"
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_certificate.py request

      - name: cert-status-media — что с сертификатом media
        if: inputs.action == 'cert-status-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CERT_NAME: ${{ inputs.cert_name || 'abhazbereg-media-cdn' }}
          CERT_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
          CERT_NO_WWW: "1"
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_certificate.py status

      - name: dns-record-media — записи media и _acme-challenge.media в зоне .рф
        if: inputs.action == 'dns-record-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          DNS_ZONE: xn--80aacbklan7f0b.xn--p1ai.
        run: |
          if [ -z "${{ inputs.acme_value }}" ]; then
            echo "::error::Передайте acme_value из вывода cert-request-media."; exit 1
          fi
          pip install --quiet pyjwt cryptography
          RECORD_NAME=media RECORD_TYPE=CNAME \
            RECORD_VALUE=062ff875d101b985.topology.gslb.yccdn.ru. \
            python3 tools/yc_dns_record.py
          RECORD_NAME=_acme-challenge.media RECORD_TYPE=CNAME \
            RECORD_VALUE="${{ inputs.acme_value }}" \
            python3 tools/yc_dns_record.py

      - name: cdn-create-media — CDN-ресурс поверх бакета медиа
        if: inputs.action == 'cdn-create-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
          CDN_CERT_NAME: abhazbereg-media-cdn
          CDN_GROUP_NAME: abhazbereg-media-origin
          CDN_ORIGIN_HOST: abhazbereg-media.storage.yandexcloud.net
          CDN_NO_WWW: "1"
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py create

      # Спасательный шаг: старый сертификат застрял в RENEWING без challenge —
      # выпускается новый под другим именем (inputs.cert_name), и этот шаг
      # перепривязывает существующий CDN-ресурс на него. DNS не трогается.
      - name: cdn-cert-media — перепривязать медиа-CDN к сертификату cert_name
        if: inputs.action == 'cdn-cert-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
          CDN_CERT_NAME: ${{ inputs.cert_name || 'abhazbereg-media-cdn' }}
          CDN_GROUP_NAME: abhazbereg-media-origin
          CDN_ORIGIN_HOST: abhazbereg-media.storage.yandexcloud.net
          CDN_NO_WWW: "1"
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py cert

      - name: cdn-status-media — состояние медиа-CDN
        if: inputs.action == 'cdn-status-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
          CDN_CERT_NAME: abhazbereg-media-cdn
          CDN_GROUP_NAME: abhazbereg-media-origin
          CDN_ORIGIN_HOST: abhazbereg-media.storage.yandexcloud.net
          CDN_NO_WWW: "1"
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py status

      - name: media-cdn-verify — фото и WebP через media.абхазберег.рф на всех узлах
        if: inputs.action == 'media-cdn-verify'
        run: |
          CN="062ff875d101b985.topology.gslb.yccdn.ru"
          IPS=$( (for i in 1 2 3 4 5 6; do dig +short "$CN" @8.8.8.8; dig +short "$CN" @77.88.8.8; sleep 1; done) \
            | grep -E "^[0-9.]+$" | sort -u)
          echo "Адреса балансировщика:"; echo "$IPS" | sed 's/^/  /'
          H="media.xn--80aacbklan7f0b.xn--p1ai"
          FAIL=0
          check_media() {
            IP="$1"; P="$2"; WANT_TYPE="$3"
            OUT=$(curl -sSI --max-time 30 --resolve "$H:443:$IP" "https://$H$P" 2>&1 || true)
            CODE=$(echo "$OUT" | grep -i "^HTTP" | awk '{print $2}' | head -1)
            TYPE=$(echo "$OUT" | grep -i "^content-type" | head -1 | tr -d '\r')
            echo "  $P → код ${CODE:-нет}, $TYPE"
            case "$CODE $TYPE" in *"200"*"$WANT_TYPE"*) ;; *) echo "     ^^ ПЛОХО"; FAIL=1;; esac
          }
          # Видео телефон запрашивает кусками (Range) — проверяем 206 и заголовки.
          check_video() {
            IP="$1"; P="$2"
            OUT=$(curl -sS -D - -o /dev/null --max-time 60 -H "Range: bytes=0-1023" \
              --resolve "$H:443:$IP" "https://$H$P" 2>&1 || true)
            CODE=$(echo "$OUT" | grep -i "^HTTP" | awk '{print $2}' | head -1)
            TYPE=$(echo "$OUT" | grep -i "^content-type" | head -1 | tr -d '\r')
            RANGE=$(echo "$OUT" | grep -i "^content-range" | head -1 | tr -d '\r')
            CACHE=$(echo "$OUT" | grep -i "^cache-control" | head -1 | tr -d '\r')
            echo "  $P"
            echo "     код ${CODE:-нет}; $TYPE; $RANGE; $CACHE"
            case "$CODE" in 206|200) ;; *) echo "     ^^ ПЛОХО: видео не отдаётся кусками"; FAIL=1;; esac
          }
          for IP in $IPS; do
            echo
            echo "=== Узел $IP ==="
            check_media "$IP" "/media/branding/logo-emblem-160.png" "image/png"
            check_media "$IP" "/media/branding/favicon-48.png" "image/png"
            check_video "$IP" "/media/videos/hotels/bungalo-glemping-3623/video-01.mp4"
            check_video "$IP" "/media/videos/hotels/mokko-briz-dva-domika-4152/video-01.mp4"
          done
          echo
          echo "=== Для сравнения: то же видео напрямую из хранилища ==="
          curl -sS -D - -o /dev/null --max-time 60 -H "Range: bytes=0-1023" \
            "https://storage.yandexcloud.net/abhazbereg-media/media/videos/hotels/bungalo-glemping-3623/video-01.mp4" 2>&1 \
            | grep -iE "^(HTTP|content-type|content-range|cache-control)" || true
          [ "$FAIL" = "0" ] && echo "ИТОГ: медиа-CDN отвечает на всех узлах." \
            || { echo "ИТОГ: есть плохие узлы."; exit 1; }

      # Телефоны/почта гостей в банках отзывов на CDN: чистим прямо в бакете,
      # затем сбрасываем кэш медиа-CDN, чтобы гости получили чистые версии.
      - name: reviews-scrub — вычистить персональные данные из банков отзывов
        if: inputs.action == 'reviews-scrub'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
        run: |
          pip install --quiet boto3 pyjwt cryptography
          python3 tools/scrub_cdn_review_banks.py | tee /tmp/scrub.log
          # Кэш CDN сбрасываем только если что-то вычищено: пустой сброс
          # зря студит кэш фото и видео для гостей.
          if grep -q "файлов с находками 0" /tmp/scrub.log; then
            echo "Банки чистые — кэш CDN не трогаем."
          else
            python3 tools/yc_cdn_tune_media.py
          fi

      - name: cdn-tune-media — донастройка медиа-CDN (кэш браузера, slice)
        if: inputs.action == 'cdn-tune-media'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: media.xn--80aacbklan7f0b.xn--p1ai
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn_tune_media.py

      # Настоящий браузер с мобильной эмуляцией жмёт play на каждом видео
      # прод-страниц и сообщает readyState/ошибку плеера + сетевые сбои.
      - name: browser-media-check — видео глазами телефона
        if: inputs.action == 'browser-media-check'
        timeout-minutes: 12
        run: |
          npm install --no-save playwright-core >/dev/null 2>&1
          node tools/browser_media_check.mjs \
            "https://xn--80aacbklan7f0b.xn--p1ai/hotels/villa-lyubov-vyhod-iz-otelya-srazu-na-plyazh-2716/" \
            "https://xn--80aacbklan7f0b.xn--p1ai/hotels/bungalo-glemping-3623/" \
            "https://xn--80aacbklan7f0b.xn--p1ai/kvartira/akira-2k-kvartira-1623/"

      - name: serverless-list — мёртвое серверлес-наследие (только посмотреть)
        if: inputs.action == 'serverless-list'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_serverless_cleanup.py list

      - name: serverless-delete — удалить мёртвое серверлес-наследие (НЕОБРАТИМО)
        if: inputs.action == 'serverless-delete'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_serverless_cleanup.py delete

      - name: cdn-status-com — состояние CDN редиректора
        if: inputs.action == 'cdn-status-com'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          CDN_DOMAIN: abhazbereg.com
          CDN_CERT_NAME: abhazbereg-com
          CDN_GROUP_NAME: abhazbereg-com-origin
          CDN_ORIGIN_HOST: abhazbereg-com-redirect.website.yandexcloud.net
        run: pip install --quiet pyjwt cryptography && python3 tools/yc_cdn.py status

      # Проверка живого домена после переключения DNS: то, чего не покрывают
      # smoke (он не смотрит файлы верификации) и availability (только корень).
      - name: domain-check — домен после переключения
        if: inputs.action == 'domain-check'
        run: |
          BASE="https://xn--80aacbklan7f0b.xn--p1ai"
          FAIL=0
          # Порог размера маленький: файл подтверждения Google — это одна
          # строка, 54 байта, и это его нормальный вид.
          check() {
            CODE=$(curl -sS -o /tmp/body -w "%{http_code}" --max-time 30 "$1" 2>/dev/null || echo 000)
            SIZE=$(wc -c < /tmp/body 2>/dev/null || echo 0)
            if [ "$CODE" = "200" ] && [ "$SIZE" -gt 20 ]; then
              echo "OK    $1 (код $CODE, $SIZE байт)"
            else
              echo "ПЛОХО $1 (код $CODE, $SIZE байт)"; FAIL=1
            fi
          }
          check "$BASE/"
          check "$BASE/yandex_983ef30d774036c7.html"
          check "$BASE/googlec85b3e07af80e239.html"
          check "$BASE/styles.min.css"
          check "$BASE/karta/"
          check "$BASE/vezu/"
          check "$BASE/gagra/"
          check "$BASE/data/catalog-index.json"
          check "https://www.xn--80aacbklan7f0b.xn--p1ai/"
          # Карта: гости видят объекты, только если точек сотни (11.08 их
          # стало 24, и карта выглядела пустой).
          POINTS=$(curl -sS --max-time 30 "$BASE/data/objects-map-points.json" \
            | python3 -c "import json,sys;print(len(json.load(sys.stdin).get('points') or []))" 2>/dev/null || echo 0)
          if [ "$POINTS" -ge 150 ]; then
            echo "OK    точек на карте: $POINTS (нужно ≥150)"
          else
            echo "ПЛОХО точек на карте: $POINTS (нужно ≥150)"; FAIL=1
          fi
          echo
          echo "=== кто отдаёт сайт (заголовок Server) ==="
          curl -sSI --max-time 30 "$BASE/" | grep -iE "^(server|via):" || true
          exit $FAIL

      - name: cdn-status — состояние ресурса
        if: inputs.action == 'cdn-status'
        env:
          YC_SA_KEY_JSON: ${{ secrets.YC_SA_KEY_JSON }}
          YC_FOLDER_ID: ${{ secrets.YC_FOLDER_ID }}
          SITE_BUCKET: ${{ inputs.bucket }}
        run: python3 tools/yc_cdn.py status

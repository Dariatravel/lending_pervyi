.DS_Store
.venv/
.venv-banner/
__pycache__/
*.pyc

# Supabase service role (never commit)
.env.supabase.local

# Google Sheets service account (never commit)
google-service-account.json

# local telegram auth/session artifacts
tg_session.session
tg_session.session.bak*
telegram_login_qr.png
tg_login_check.py
tg_export_topics.py.save

# local Supabase secrets
.env.supabase.local

# local Yandex Object Storage secrets
.env.yandex.local

# Telegram site update bot secrets
.env.site-update-bot

# generated heavy telegram source videos (intermediate, not for git)
media/videos/**/video-*-source.mp4
